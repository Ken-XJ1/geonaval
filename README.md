
  # Interfaz web para GEONAVAL (copia)

  This is a code bundle for Interfaz web para GEONAVAL (copia). The original project is available at https://www.figma.com/design/u0Z8YPiRcr7QpizacVXhJN/Interfaz-web-para-GEONAVAL--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  